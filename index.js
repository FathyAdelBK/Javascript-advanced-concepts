const number = 610;
const string = "some text";

const human = {
  first: "Andrie",
  last: "Neagoie",
};

function subtractTwo(num) {
  return num - 2;
}

function calculate() {
  const sumTotal = 4 + 5;
  return subtractTwo(sumTotal);
}

calculate();
